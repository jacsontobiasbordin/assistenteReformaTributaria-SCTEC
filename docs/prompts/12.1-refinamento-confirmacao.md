# Prompt 12.1 — Refinamento: confirmação em vez de aprovação de terceiro

- **Data de execução:** 2026-08-25
- **Branch:** fix/confirmacao-vs-aprovacao
- **Issue:** [#33](https://github.com/jacsontobiasbordin/assistenteReformaTributaria-SCTEC/issues/33)

## Prompt integral

```
A Etapa 12 (automação low-code via n8n) já está implementada e mesclada
em develop. Depois de uma revisão de design, identificamos um problema de
linguagem, não de arquitetura: o texto "aprovação humana" e o botão
"Aprovar e notificar" sugerem que uma pessoa DIFERENTE do solicitante
revisa a análise antes de aprovar — mas isso não é verdade nesta versão
(não há autenticação multiusuário; quem pergunta é quem confirma). A
mecânica de segurança continua correta (nenhuma notificação sai sem uma
ação humana explícita); só a comunicação disso precisa mudar de
"aprovação" para "confirmação", e a limitação precisa ficar documentada
com honestidade.

IMPORTANTE: este prompt é sobre ALTERAR código e arquivos que JÁ EXISTEM
no repositório, não sobre criar a funcionalidade do zero. Antes de cada
passo, localize o arquivo/trecho já implementado e modifique-o — não
recrie um arquivo já existente do zero, e não duplique lógica.

PASSO 1 — CRIAR O CARD/ISSUE DESTE REFINAMENTO

  REPO="jacsontobiasbordin/assistenteReformaTributaria-SCTEC"
  OWNER="jacsontobiasbordin"
  PROJECT_NUMBER="2"

  URL=$(gh issue create -R "$REPO" \
    --title "Etapa 12.1 — Refinamento: confirmacao em vez de aprovacao de terceiro" \
    --body $'Objetivo: revisar a linguagem do portao de notificacao (Etapa 12/#13) de "aprovacao humana" para "confirmacao de envio", corrigindo a sugestao enganosa de revisao por terceiro, e documentar a limitacao.\nResultado esperado: mensagens, endpoint, botao e fluxo n8n renomeados; limitacao documentada no README.\nBranch: fix/confirmacao-vs-aprovacao' \
    --label "seguranca")

  gh project item-add "$PROJECT_NUMBER" --owner "$OWNER" --url "$URL"
  echo "Issue criada: $URL"

Anote o número retornado e substitua `#N` por ele em todos os commits e
no PR a partir daqui. Mova o card para **Em Andamento** no Project.

git checkout develop
git pull origin develop
git checkout -b fix/confirmacao-vs-aprovacao

Execute as etapas abaixo — todas sobre código JÁ EXISTENTE:

1. LOCALIZAR E ALTERAR A MENSAGEM DO PORTÃO (app/agent/nodes.py)
   Encontre o node `solicitar_aprovacao_humana` (implementado na Etapa
   7) e troque o texto atual do campo `aviso_aprovacao` — algo como
   "requer aprovação humana antes de qualquer notificação externa ser
   disparada" — por:
     "Esta análise envolve cálculo de impostos. Confirme para notificar
     a área fiscal responsável; nenhuma notificação é enviada
     automaticamente."
   NÃO renomeie o campo de estado `aguardando_aprovacao_humana` nem o
   nome da função `solicitar_aprovacao_humana` — eles continuam
   descrevendo corretamente o comportamento interno; só o texto voltado
   ao usuário muda.
   Localize o teste que verifica esse texto (provavelmente em
   `tests/test_seguranca.py`) e atualize a asserção para o novo texto.

2. LOCALIZAR E RENOMEAR O ENDPOINT (app/web/main.py)
   Encontre a rota `POST /api/aprovar` (e o schema `AprovarRequest`,
   provavelmente em `app/web/schemas.py` ou no próprio `main.py`) e
   renomeie para `POST /api/confirmar-notificacao` com o schema
   `ConfirmarNotificacaoRequest`. Mantenha exatamente a mesma lógica
   interna (verificação de `aguardando_aprovacao_humana`, chamada a
   `disparar_notificacao`, tratamento de `NotificacaoFalhouError`) — é
   uma renomeação, não uma reimplementação. Busque e atualize TODAS as
   referências a `/api/aprovar` no projeto (testes, front-end, README).

3. LOCALIZAR E ATUALIZAR OS TESTES DO ENDPOINT (tests/test_web.py)
   Atualize os testes existentes que chamam `/api/aprovar` para chamarem
   `/api/confirmar-notificacao`, mantendo as mesmas verificações (400 sem
   confirmação pendente, 200 com confirmação pendente e o mock de
   `disparar_notificacao` sendo chamado corretamente).

4. LOCALIZAR E ATUALIZAR O FRONT-END (app/web/static/app.js e
   app/web/static/index.html)
   Encontre o botão que hoje diz "Aprovar e notificar" (dentro do banner
   de `aguardando_aprovacao_humana`) e:
   - troque o texto para "Confirmar e notificar a área fiscal";
   - atualize a chamada JS de `POST /api/aprovar` para
     `POST /api/confirmar-notificacao`.
   Não altere mais nada da estrutura da tela.

5. ATUALIZAR O FLUXO NO N8N (passo MANUAL, feito por você na interface
   do n8n — a IA não tem acesso ao n8n rodando em Docker)
   Com o ambiente local do n8n no ar (`cd n8n && docker compose up -d`),
   abra o fluxo já existente e:
   - no node **Webhook**, troque o path de `/reformatax-aprovacao` para
     `/reformatax-confirmacao`;
   - no node **Set/Edit Fields**, ajuste a mensagem para refletir
     confirmação em vez de aprovação (ex.: trocar "Analise aprovada" por
     "Notificacao confirmada pelo usuario");
   - reexporte o fluxo: menu (⋯) → Download.

6. SUBSTITUIR O ARQUIVO EXPORTADO NO REPOSITÓRIO
   Depois de reexportar manualmente (passo 5), peça para a IA:
   - remover `n8n/fluxo-aprovacao-reformatax.json` (git rm);
   - adicionar o novo arquivo exportado como
     `n8n/fluxo-confirmacao-reformatax.json`;
   - atualizar qualquer referência a `fluxo-aprovacao-reformatax.json`
     no README para o novo nome de arquivo e para o novo path do
     webhook (`/reformatax-confirmacao`).

7. CRIAR O DOCUMENTO DE REFINAMENTO (docs/qa/refinamento-confirmacao-vs-aprovacao.md
   — este SIM é um arquivo novo)
   Registre, no formato problema/alteração/resultado (requisito 4.10):
   - Problema observado: o texto "aprovação humana" e o botão "Aprovar e
     notificar" sugeriam revisão por um terceiro distinto do
     solicitante — o que não reflete o comportamento real da aplicação
     (sem autenticação multiusuário, quem pergunta é quem confirma);
   - Alteração realizada: mensagem do node, endpoint, front-end e fluxo
     do n8n renomeados/revisados para a linguagem de "confirmação de
     envio"; limitação documentada explicitamente no README;
   - Resultado obtido: a interface agora comunica com precisão o nível
     de controle que realmente existe.

8. LOCALIZAR E ATUALIZAR O README.md
   - Na seção "Segurança e autonomia" (já existente, da Etapa 7),
     adicione o parágrafo de limitação: "Nesta versão, o mesmo usuário
     que formula a pergunta é quem confirma o envio da notificação — não
     há papel de revisor/aprovador distinto, pois o projeto não
     implementa autenticação multiusuário. Em um cenário de produção
     real, esse papel pertenceria a um analista fiscal ou gestor
     diferente do solicitante.";
   - Na seção "Automação low-code (n8n)" (já existente, da Etapa 12),
     atualize todas as referências: nome do arquivo JSON, path do
     webhook, texto do botão, nome do endpoint.

9. REGISTRAR O PROMPT (arquivo novo)
   Crie `docs/prompts/12.1-refinamento-confirmacao.md` com o texto
   integral deste prompt, incluindo o número real da issue criada no
   passo 1.

10. COMMITS SEMÂNTICOS (troque #N pelo número real da issue)
    1. fix: revisa mensagem do portao para linguagem de confirmacao (#N)
    2. refactor: renomeia endpoint de aprovar para confirmar-notificacao (#N)
    3. test: atualiza testes do endpoint renomeado (#N)
    4. feat: atualiza front-end com texto e chamada de confirmacao (#N)
    5. chore: substitui o fluxo n8n exportado por fluxo-confirmacao-reformatax.json (#N)
    6. docs: documenta o refinamento de linguagem confirmacao vs aprovacao (#N)
    7. docs: atualiza README com a limitacao e as referencias renomeadas (#N)
    8. docs: registra o prompt 12.1 em docs/prompts/12.1-refinamento-confirmacao.md (#N)

11. ENVIAR A BRANCH E ABRIR O PULL REQUEST
    git push -u origin fix/confirmacao-vs-aprovacao

    Mova o card para **Em Revisão** no Project ao abrir o PR.

    Abra o PR direcionado para develop (troque #N pelo número real):
      Título: "fix: linguagem de confirmacao em vez de aprovacao de terceiro (#N)"
      Corpo:
        Closes #N

        ## Contexto
        Refina a Etapa 12 (#13), ja implementada: corrige a linguagem de
        "aprovacao humana" para "confirmacao de envio", ja que nao ha
        revisor distinto do solicitante nesta versao, e documenta essa
        limitacao com honestidade.

        ## O que foi alterado (nao e funcionalidade nova)
        - Mensagem do node solicitar_aprovacao_humana
        - Endpoint /api/aprovar -> /api/confirmar-notificacao
        - Botao do front-end e chamada JS correspondente
        - Fluxo do n8n (path do webhook e mensagem) + arquivo exportado
        - README (limitacao documentada + referencias atualizadas)

        ## Fora do escopo deste PR
        - Nenhuma mudanca de arquitetura ou de logica de negocio

        ## Checklist
        - [x] Nenhuma referencia antiga (/api/aprovar, nome de arquivo antigo) restante no projeto
        - [x] Testes atualizados e passando
        - [x] Limitacao documentada no README

12. VALIDAÇÃO FINAL
    Rode `pytest tests/ -v` e confirme que passa. Busque no projeto
    inteiro por `api/aprovar`, `Aprovar e notificar` e
    `fluxo-aprovacao-reformatax` (`grep -r` ou busca do editor) e
    confirme que não sobrou nenhuma referência antiga.

Este é um refinamento pontual — não implemente nada além do que está
listado acima.
```

## Resultado obtido

Ver [docs/qa/refinamento-confirmacao-vs-aprovacao.md](../qa/refinamento-confirmacao-vs-aprovacao.md)
para o registro problema/alteração/resultado deste ciclo.
